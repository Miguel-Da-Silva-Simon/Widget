package com.example.widget_android.data

/** Descanso 30 min; comida 1 h (ajustable si el backend expone duración). */
object AttendanceDurations {
    const val BREAK_MS = 30L * 60 * 1000
    const val MEAL_MS = 60L * 60 * 1000
}
